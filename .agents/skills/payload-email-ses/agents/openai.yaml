interface:
  display_name: "Payload Email Ses"
  short_description: "Repo-specific patterns and workflows for payload-email-ses"
  default_prompt: "Use the payload-email-ses repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true